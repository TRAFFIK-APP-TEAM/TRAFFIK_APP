<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TRAFFIK_APP.Views.BookingVehicleSelectPage"
             BackgroundColor="#0D0D0D"
             Shell.TabBarIsVisible="False"
             Title="Select Vehicle">

    <Grid RowDefinitions="Auto,*,Auto" Padding="20">

        <!-- Header -->
        <StackLayout Grid.Row="0" Margin="0,20,0,20">
            <Label Text="TRAFFIK"
                   TextColor="White"
                   FontAttributes="Bold"
                   FontSize="22"/>
            <Label Text="Choose Your Vehicle"
                   TextColor="#C8C8C8"
                   FontSize="16"
                   Margin="0,10,0,0"/>
        </StackLayout>

        <!-- Content -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="15">

                <!-- Three placeholder clickable frames -->
                <Frame BackgroundColor="#1E1E1E"
                       CornerRadius="15"
                       Padding="20"
                       Margin="0,5"
                       HasShadow="False">
                    <Button Text="Vehicle Option 1"
                            BackgroundColor="#007AFF"
                            TextColor="White"
                            CornerRadius="10"/>
                </Frame>

                <Frame BackgroundColor="#1E1E1E"
                       CornerRadius="15"
                       Padding="20"
                       Margin="0,5"
                       HasShadow="False">
                    <Button Text="Vehicle Option 2"
                            BackgroundColor="#007AFF"
                            TextColor="White"
                            CornerRadius="10"/>
                </Frame>

                <Frame BackgroundColor="#1E1E1E"
                       CornerRadius="15"
                       Padding="20"
                       Margin="0,5"
                       HasShadow="False">
                    <Button Text="Vehicle Option 3"
                            BackgroundColor="#007AFF"
                            TextColor="White"
                            CornerRadius="10"/>
                </Frame>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Bottom Navigation Bar (same as BookingPage) -->
        <Grid Grid.Row="2"
              BackgroundColor="#1E1E1E"
              HeightRequest="70"
              Padding="10,0"
              ColumnSpacing="25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackLayout Grid.Column="0" HorizontalOptions="Center">
                <ImageButton Source="home.png"
                             BackgroundColor="Transparent"
                             HeightRequest="28" WidthRequest="28"/>
                <Label Text="Home" FontSize="12" TextColor="White" HorizontalOptions="Center"/>
            </StackLayout>

            <StackLayout Grid.Column="1" HorizontalOptions="Center">
                <ImageButton Source="appointment.png"
                             BackgroundColor="Transparent"
                             HeightRequest="28" WidthRequest="28"/>
                <Label Text="Appointment" FontSize="12" TextColor="#007AFF" HorizontalOptions="Center"/>
            </StackLayout>

            <StackLayout Grid.Column="2" HorizontalOptions="Center">
                <ImageButton Source="rewards.png"
                             BackgroundColor="Transparent"
                             HeightRequest="28" WidthRequest="28"/>
                <Label Text="Rewards" FontSize="12" TextColor="White" HorizontalOptions="Center"/>
            </StackLayout>

            <StackLayout Grid.Column="3" HorizontalOptions="Center">
                <ImageButton Source="account.png"
                             BackgroundColor="Transparent"
                             HeightRequest="28" WidthRequest="28"/>
                <Label Text="Account" FontSize="12" TextColor="White" HorizontalOptions="Center"/>
            </StackLayout>
        </Grid>
    </Grid>
</ContentPage>
