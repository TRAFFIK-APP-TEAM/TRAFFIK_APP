<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TRAFFIK_APP.Views.AdminProfilePage"
             BackgroundColor="Black"
             Shell.TabBarIsVisible="False"
             Title="Profile">

    <Grid RowDefinitions="*,Auto">
        <ScrollView Grid.Row="0">
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- HEADER -->
            <Label Text="Profile"
                   TextColor="White"
                   FontSize="24"
                   FontAttributes="Bold"/>

            <!-- Greeting -->
            <VerticalStackLayout>
                <Label Text="Welcome back," TextColor="White" FontSize="16"/>
                <Label Text="{Binding FullName}" TextColor="White" FontAttributes="Bold" FontSize="18"/>
            </VerticalStackLayout>

            <!-- PROFILE INFO -->
            <Border Background="#1E1E1E" Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15"/>
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="15">
                    <Label Text="Profile Information" FontSize="18" FontAttributes="Bold"
                           TextColor="White" HorizontalOptions="Center"/>

                    <Label Text="Name:" TextColor="#C8CDD6" FontSize="14"/>
                    <Label Text="{Binding FullName}" TextColor="White" FontSize="16" FontAttributes="Bold"/>

                    <Label Text="Email:" TextColor="#C8CDD6" FontSize="14"/>
                    <Label Text="{Binding Email}" TextColor="White" FontSize="16"/>

                    <!--<Label Text="Phone:" TextColor="#C8CDD6" FontSize="14"/>
                    <Label Text="{Binding PhoneNumber}" TextColor="White" FontSize="16"/>-->

                    <Label Text="Role:" TextColor="#C8CDD6" FontSize="14"/>
                    <Label Text="Administrator" TextColor="White" FontSize="16" FontAttributes="Bold"/>
                </VerticalStackLayout>
            </Border>

            <!-- LOGOUT BUTTON -->
            <Button Text="Logout"
                    Command="{Binding LogoutCommand}"
                    BackgroundColor="#DC3545"
                    TextColor="White"
                    FontAttributes="Bold"
                    CornerRadius="12"
                    HeightRequest="48"
                    Margin="0,10,0,20"/>
        </VerticalStackLayout>
    </ScrollView>
    </Grid>

</ContentPage>

